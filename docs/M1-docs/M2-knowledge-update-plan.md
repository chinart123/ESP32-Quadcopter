# Hướng dẫn đọc tài liệu chuyên sâu – Kiến trúc Bus & Ngoại vi
## Dành cho Quadcopter Mini (STM32F103C8T6 & ESP32-S3 Supermini)
### Tập trung: I2C với DMA + Ngắt, kết hợp FreeRTOS

---

## 1. STM32F103C8T6

| Chương | Mục chính | Nội dung cần tập trung | Mục đích (2 trạng thái) | Ghi chú (đường đi trong kiến trúc bus) | Tài liệu & Link |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **26 – I2C interface** | 26.3.6 DMA requests | Các bit `DMAEN`, `LAST` trong `I2C_CR2`. Cơ chế gửi request từ I2C đến DMA controller (DMA1_Channel7 cho RX). Tác động trực tiếp đến **DMA1 (kênh 7)** và **bus matrix AHB‑APB1** (I2C1 là slave APB1). | **Trạng thái 1 – Kiến trúc:** Khi `DMAEN=1`, mỗi khi byte được nhận vào `I2C_DR`, peripheral I2C phát tín hiệu request lên DMA controller. DMA đọc `I2C_DR` qua cầu AHB‑to‑APB1 và ghi vào địa chỉ RAM đã lập trình, không qua CPU. Nếu `LAST=1`, sau byte cuối cùng, I2C tự sinh STOP. Ngắt DMA‑TC báo hiệu hoàn tất burst.<br>**Trạng thái 2 – Drone:** Đọc burst 14 byte MPU6050 bằng DMA giúp CPU rảnh tay trong suốt 0.3 ms truyền I2C. Kết hợp với ngắt EXTI từ chân INT, flight task chỉ xử lý một lần khi toàn bộ dữ liệu đã có, duy trì chính xác chu kỳ 500 Hz. | **Clock:** HSI/PLL → AHB Prescaler → APB1 Prescaler (≤36MHz) → I2C1 peripheral clock. **Data RX:** I2C1 (APB1 slave) → DMA request → DMA1_Channel7 (AHB master) → Bus Matrix → SRAM. | **RM0008** [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| | 26.3.7 Packet Error Checking | Bit `PEC` trong `I2C_CR1`. Cơ chế tính CRC8 tự động trên dữ liệu nhận. Liên quan đến **I2C hardware calculator** nằm trong chính peripheral I2C. | **Trạng thái 1 – Kiến trúc:** Khi `PEC=1`, sau mỗi byte nhận, I2C tự động cập nhật thanh ghi `PECR`. Sau byte cuối, giá trị PEC được truyền đi. Đây là tính năng phần cứng, không tốn chu kỳ CPU.<br>**Trạng thái 2 – Drone:** Có thể kích hoạt PEC để phát hiện lỗi đường truyền do nhiễu từ động cơ. Khi PEC sai, DMA vẫn hoàn thành nhưng firmware kiểm tra và loại bỏ frame hỏng, tránh tính toán góc sai gây rung lắc drone. | I2C peripheral internal PEC calculator (nằm ngay trong I2C, không tác động đến bus matrix hay DMA). | [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| **13 – DMA controller** | 13.3.5 DMA channels (Table 78) | Xác định kênh DMA cho I2C1_RX: **DMA1_Channel7**. Cấu hình `DMA_CPAR` = địa chỉ `I2C1->DR`, `DMA_CMAR` = địa chỉ buffer. Tác động đến **DMA1 (AHB slave)** và **bus matrix** (Hình 2 trong Ch.2). DMA1 được kết nối vào bus AHB hệ thống, dùng chung với CPU để truy cập SRAM qua System Bus. | **Trạng thái 1 – Kiến trúc:** DMA1_Channel7 hoạt động như một bộ điều khiển truy cập bộ nhớ trực tiếp. Khi được kích hoạt, nó chiếm quyền bus từ CPU (cycle stealing) để đọc `I2C_DR` rồi ghi vào SRAM. Không cần lưu/khôi phục ngữ cảnh CPU, giảm độ trễ.<br>**Trạng thái 2 – Drone:** Nhờ DMA, mỗi byte I2C không làm gián đoạn flight loop. CPU chỉ bị ngắt một lần duy nhất khi đủ 14 byte, sau đó xử lý dữ liệu và cập nhật PWM motor, đảm bảo jitter thấp nhất có thể. | I2C_RX request → DMA1_Channel7 (AHB slave, đọc thanh ghi I2C_DR qua APB1 bridge) → AHB system bus → SRAM (ghi vào buffer). | [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| | 13.4.5 DMA interrupt (TC, HT, TE) | Cờ `TCIF7` (Transfer Complete), `HTIF7` (Half Transfer), `TEIF7` (Transfer Error) trong `DMA_ISR`. Cấu hình ngắt NVIC cho DMA1_Channel7. Tác động đến **NVIC** và **Cortex‑M3 core**. | **Trạng thái 1 – Kiến trúc:** Khi DMA hoàn tất truyền, nó set cờ TCIF7 và gửi tín hiệu ngắt tới NVIC. Cortex‑M3 vào ISR, tại đây firmware chỉ cần đọc cờ và gửi semaphore (nếu dùng RTOS).<br>**Trạng thái 2 – Drone:** Trong flight task, ngắt DMA TC được dùng để mở khóa semaphore, báo hiệu dữ liệu IMU đã sẵn sàng. Flight task chạy ở Core 1 (nếu là ESP32) hoặc trong main loop, không phải chờ I2C. | DMA1_Channel7 → set cờ TCIF7 → NVIC interrupt → Cortex-M3 core (ISR) → set semaphore/biến cờ cho flight task. | [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| **9 – Interrupts and events** (NVIC) | Cấu hình ưu tiên NVIC, priority grouping. | Chọn nhóm ưu tiên `NVIC_PriorityGroup_4` cho bare‑metal. Với FreeRTOS, dùng `NVIC_PriorityGroup_4` và đặt DMA interrupt ưu tiên thấp hơn EXTI. Tác động đến **NVIC controller** trong Cortex‑M3. | **Trạng thái 1 – Kiến trúc:** NVIC sắp xếp thứ tự phục vụ ngắt dựa trên priority group. Đặt DMA ưu tiên thấp hơn ngắt ngoài (EXTI) để khi MPU INT đến, nó được phục vụ ngay lập tức, kích hoạt DMA.<br>**Trạng thái 2 – Drone:** EXTI (ngắt từ chân INT MPU) phải có ưu tiên cao nhất để bắt đầu burst DMA đúng nhịp 500 Hz. DMA TC có ưu tiên thấp hơn để không trì hoãn flight task chính. | NVIC nhận tín hiệu ngắt từ DMA1_Channel7 và EXTI. Cortex-M3 lõi xử lý ngắt theo priority. Không thay đổi đường bus. | [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| **8 – EXTI** | Cấu hình chân GPIO làm ngắt ngoài (EXTI line), trigger edge. | Chân PAx (ví dụ PA8) kết nối MPU INT, cấu hình `EXTI_Trigger_Rising`. Tác động đến **EXTI controller** và **APB2 bus**. | **Trạng thái 1 – Kiến trúc:** Khi chân INT chuyển mức, EXTI phát hiện cạnh, gửi tín hiệu ngắt tới NVIC. ISR phải cực ngắn: chỉ cần tắt EXTI, gọi `xSemaphoreGiveFromISR()` hoặc kích hoạt DMA.<br>**Trạng thái 2 – Drone:** Mỗi xung INT đúng 2 ms đánh dấu một frame dữ liệu mới. Flight task dùng semaphore để thức dậy, đọc dữ liệu từ buffer DMA và bắt đầu tính toán PID. | GPIO pin → EXTI controller (APB2 slave) → NVIC → Core. EXTI không dùng DMA. | [PDF (ST)](https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf) |
| **Datasheet STM32F103x8** | Pin definitions (Table 8) | Xác định chân PB6 (SCL), PB7 (SDA) cho I2C1, và chân PAx cho INT. Tác động đến **GPIO peripheral** và **alternate function mapping** (AFIO). | **Trạng thái 1 – Kiến trúc:** Các chân được cấu hình alternate function open‑drain để hoạt động I2C. Bên trong chip, tín hiệu được dẫn đến I2C peripheral qua ma trận GPIO.<br>**Trạng thái 2 – Drone:** Chọn đúng chân và kiểm tra xung đột với PWM motor (TIM3_CHx) để tránh chập phần cứng. Nếu dùng PA8 cho INT, cần đảm bảo TIM1 không dùng kênh đó. | GPIO pin logic → I/O pad → Alternate Function Output (I2C) hoặc Input (EXTI). Không qua bus matrix. | [PDF (ST)](https://www.st.com/resource/en/datasheet/stm32f103c8.pdf) |
| **Errata sheet** | I2C peripheral limitations | Đọc tất cả các mục về I2C: lỗi SDA stuck low, BTF không set, DMA kết hợp LAST. Tác động trực tiếp đến thiết kế **I2C silicon** và driver firmware. | **Trạng thái 1 – Kiến trúc:** Lỗi phần cứng buộc firmware phải thêm workaround: sử dụng bit LAST cho DMA, đọc SR1/SR2 để clear cờ đúng thứ tự, và triển khai bus recovery bằng bit‑banging.<br>**Trạng thái 2 – Drone:** Nếu không áp dụng workaround, drone có thể bị treo giữa không trung. Driver mới phải bao gồm cơ chế phục hồi bus và timeout để bảo vệ flight loop. | I2C peripheral internal state machine (không thay đổi bus path). | [PDF (ST)](https://www.st.com/resource/en/errata_sheet/es096-stm32f103x8-and-stm32f103xb-device-errata-stmicroelectronics.pdf) |

---

## 2. ESP32‑S3 Supermini (ESP‑IDF + FreeRTOS)

| Chương | Mục chính | Nội dung cần tập trung | Mục đích (2 trạng thái) | Ghi chú (đường đi trong kiến trúc bus) | Tài liệu & Link |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TRM Ch.27 – I2C Controller** | 27.3 Functional description, 27.4.2 DMA Interface | Kiến trúc I2C với RAM‑based command table (RTC), GDMA request. Tác động đến **I2C peripheral** (APB slave), **GDMA controller**, và **bus matrix** (XEM Hình 3‑2 trong TRM Ch.3). | **Trạng thái 1 – Kiến trúc:** I2C peripheral đọc các lệnh từ RAM nội (RTC) để tự động thực hiện START, địa chỉ, đọc dữ liệu mà không cần CPU. Mỗi byte nhận được đẩy vào FIFO, sau đó GDMA chuyển vào SRAM qua AHB/APB bridge. GDMA hoạt động như một bus master độc lập.<br>**Trạng thái 2 – Drone:** Với GDMA và RTC, việc đọc 14 byte từ MPU hoàn toàn tự động. Flight task chỉ cần gọi API async và chờ callback; trong thời gian chờ, CPU có thể xử lý tác vụ khác hoặc ngủ. Điều này lý tưởng cho kiến trúc dual‑core: Core 1 chạy PID không bị gián đoạn. | Clock: APB clock → I2C peripheral. Data RX: I2C (APB slave) → FIFO → GDMA request → GDMA channel (AHB master) → AHB bus matrix → SRAM (Hình 3‑2). | [PDF TRM](https://www.espressif.com/sites/default/files/documentation/esp32-s3_technical_reference_manual_en.pdf) |
| **TRM Ch.6 – IO MUX and GPIO Matrix** | 6.4.2 GPIO Matrix, 6.3 IO MUX | Cấu hình chân I2C SDA, SCL và chân INT qua IO MUX để kết nối tới I2C peripheral và GPIO. Tác động đến **IO MUX, GPIO Matrix** và **Fast GPIO**. | **Trạng thái 1 – Kiến trúc:** IO MUX gán chức năng ngoại vi cho chân vật lý; GPIO Matrix định tuyến tín hiệu ngắt từ chân ngoài vào CPU. Cấu hình đúng đảm bảo tín hiệu INT tới được interrupt input của Core 1.<br>**Trạng thái 2 – Drone:** Chọn chân GPIO4 (INT) và GPIO8,9 (I2C) phải khớp với thiết kế PCB. Cấu hình pull‑up nội để chống nhiễu. Tín hiệu INT được lập trình làm ngắt ngoài, đánh thức flight task. | GPIO pin → IO MUX → I2C peripheral (chức năng SDA/SCL) hoặc GPIO Matrix → Interrupt input của CPU (INT). | [PDF TRM](https://www.espressif.com/sites/default/files/documentation/esp32-s3_technical_reference_manual_en.pdf) |
| **TRM Ch.3 – System and Memory** | 3.5 GDMA (AHB GDMA) | Kênh GDMA dành cho I2C, burst size, priority. Tác động đến **GDMA controller** (AHB bus master), **AHB/APB bridge**, và **SRAM** (Hình 3‑2). | **Trạng thái 1 – Kiến trúc:** GDMA có 5 kênh, mỗi kênh có thể cấu hình burst 1/4/8/16 byte. Khi nhận dữ liệu I2C, GDMA đọc FIFO và ghi vào SRAM theo burst, tối ưu băng thông. Priority có thể lập trình để không tranh chấp với CPU.<br>**Trạng thái 2 – Drone:** Chọn burst size phù hợp (4 byte) giúp giảm overhead. Đảm bảo GDMA không ảnh hưởng đến LEDC PWM (dùng phần cứng khác). Flight task chỉ nhận dữ liệu khi toàn bộ 14 byte đã sẵn sàng. | I2C FIFO → GDMA request → GDMA kênh x → AHB bus matrix → internal SRAM (hình 3‑2). | [PDF TRM](https://www.espressif.com/sites/default/files/documentation/esp32-s3_technical_reference_manual_en.pdf) |
| **ESP‑IDF I2C Driver (new)** | I2C Master Controller, Thread Safety, Async API | Học cách dùng `i2c_master_bus_create()`, `i2c_master_dev_handle_t`, và hàm `i2c_master_read_from_device_async()`. Tác động đến **Software framework** (ESP‑IDF) và **GDMA driver**. | **Trạng thái 1 – Kiến trúc:** Driver mới đóng gói toàn bộ việc quản lý GDMA và RTC. Hàm async trả về ngay lập tức; khi hoàn tất, nó gọi callback hoặc set semaphore. Bên trong, nó cấu hình GDMA và I2C command table, ẩn đi chi tiết phần cứng.<br>**Trạng thái 2 – Drone:** Trong flight loop, gọi `i2c_master_read_from_device_async()` với callback cập nhật biến `imu_data` và đưa semaphore. Flight task đợi semaphore với timeout cực nhỏ, đảm bảo luôn có dữ liệu mới mỗi 2 ms. | Phần mềm: App code → ESP-IDF I2C driver → GDMA driver → hardware (GDMA + I2C). Không thay đổi bus path nhưng ảnh hưởng latency. | [Trang I2C Driver](https://docs.espressif.com/projects/esp-idf/en/stable/esp32s3/api-reference/peripherals/i2c.html) |
| **ESP‑IDF GPIO & External Interrupt** | `gpio_install_isr_service()`, `gpio_isr_handler_add()` | Cấu hình chân INT (GPIO4) làm ngắt ngoài với cạnh lên. ISR chỉ gọi `xSemaphoreGiveFromISR()`. Tác động đến **GPIO ISR service** và **FreeRTOS scheduler**. | **Trạng thái 1 – Kiến trúc:** GPIO ISR service là một task riêng của ESP‑IDF, nhận ngắt từ phần cứng và gọi handler. Handler của ta phải cực ngắn, chỉ thao tác trên biến semaphore để bảo toàn thời gian thực.<br>**Trạng thái 2 – Drone:** Mỗi khi MPU INT báo hiệu, semaphore được nhả, flight task (đang block) lập tức được scheduler đánh thức. Flight task đọc dữ liệu mới và tính toán, đóng vòng loop 500 Hz. | GPIO pad → GPIO Matrix → interrupt input → CPU interrupt → ISR (ngắn) → xSemaphoreGiveFromISR → scheduler đánh thức flight task. | [Trang GPIO](https://docs.espressif.com/projects/esp-idf/en/stable/esp32s3/api-reference/peripherals/gpio.html) |
| **ESP‑IDF FreeRTOS** | Task Management, `xTaskCreatePinnedToCore`, Semaphore | Tạo flight task gắn chặt vào Core 1, ưu tiên cao. Dùng semaphore nhị phân để đồng bộ với ISR. Tác động đến **FreeRTOS kernel** và **dual‑core SMP scheduler**. | **Trạng thái 1 – Kiến trúc:** FreeRTOS SMP trên ESP32‑S3 cho phép chạy task trên bất kỳ core nào. `xTaskCreatePinnedToCore` ghim task vào Core 1, tránh xung đột với Wi‑Fi/BLE trên Core 0. Semaphore cung cấp cơ chế blocking an toàn, không tốn CPU khi chờ.<br>**Trạng thái 2 – Drone:** Flight task dành toàn bộ thời gian Core 1 cho PID. Khi không có dữ liệu mới, nó ngủ hoàn toàn, tiết kiệm năng lượng và tránh nóng chip. Phản hồi điều khiển cực nhanh vì không có polling. | Scheduler (Core 1): ISR → Semaphore → unblock flight task → PID loop → PWM update. Không thay đổi bus path. | [Trang FreeRTOS (IDF)](https://docs.espressif.com/projects/esp-idf/en/stable/esp32s3/api-reference/system/freertos_idf.html) |